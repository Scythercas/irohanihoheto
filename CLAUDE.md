# CLAUDE.md — プロジェクト開発憲章

> このファイルは**セッションをまたいでもゲームのスタイル・開発環境にブレを生じさせない**ための最上位ルールです。
> 新しいセッションを開始したClaudeは、**必ず最初にこのファイルと `docs/` 配下を読んでから**作業を開始すること。

プロジェクト名: **『いろはにほへと』**（恋愛シミュレーションゲーム）
作業ディレクトリ: `c:\Users\user\work\dev\99_claude\007_LoveSimGame`

## タイトルの意味（作品の背骨・改変禁止）

- **いろは** ＝ 主人公「色葉」／**恋愛のいろは（初歩）**
- 本歌「**色は匂へど 散りぬるを**」＝ **美しい色も散ってしまう**
  → **5人のヒロインとは自然消滅（フェードアウト）で終わる**のが本作の主題であり、妥協ではない
  → **散らずに残るのが茜だけ**、という結末の意味づけ

**この一点を外すと作品が別物になる。シナリオ・演出・宣材はすべてここに立ち返ること。**

---

## 0. セッション開始時の必須手順

1. `CLAUDE.md`（本ファイル）を読む
2. `docs/05_implementation-status.md` を読み、**何が実装済みで何が未実装か**を把握する
3. `docs/00_instruction-log.md` の**末尾**を読み、直近の指示と次アクションを把握する
4. `docs/01_game-design.md` を読み、確定事項／暫定案／未決を区別する
5. 不明点があれば**推測で進めず、ユーザーに確認する**

**シナリオを書くときは `docs/05_implementation-status.md` §3 の記法早見表を参照すること。**

## 1. 進行フェーズ（ユーザー指定・厳守）

| Phase | 内容 | 状態 |
|---|---|---|
| 1 | ゲーム内容のヒアリング・懸念点の提示・企画深堀り | **完了**（2026-08-14） |
| 2 | 開発環境の相談・技術選定 | **完了**（2026-08-14） → `docs/03_tech-stack.md` |
| 3 | 開発 | **進行中** |

**フェーズを飛ばして先走らない。** 特に Phase 1 が終わるまでコードを書き始めないこと。
（設計書・ドキュメントの作成・更新はフェーズに関係なく随時可）

## 2. ドキュメント運用ルール（最重要）

- **設計書が正、会話は補助。** 決定した内容は必ず `docs/` に反映してから次に進む。
- ユーザーからの指示は `docs/00_instruction-log.md` に**一字一句変更せず**原文を追記する。
  - コードフェンス（`~~~text`）で囲み、誤字・空白・改行も改変しない。
  - 併せてClaude側の対応内容を**要約**して記載し、会話の流れを追えるようにする。
- 仕様変更が発生したら、`docs/01_game-design.md` の該当箇所を更新し、変更履歴に1行残す。
- 「確定」「暫定」「未決」を必ずラベルで区別する。暫定案を確定事項のように書かない。

### ファイル構成

```text
CLAUDE.md                     … 本ファイル（開発憲章／セッション横断ルール）
docs/
  00_instruction-log.md       … 指示原文ログ＋Claude対応要約
  01_game-design.md           … ゲーム設計書（世界観・キャラ・システム・エンド構成）
  02_open-questions.md        … ヒアリング項目と回答の記録簿【Phase1完了】
  03_tech-stack.md            … 技術選定・開発環境・公開方法【確定済】
  04_asset-guide.md           … ★立ち絵の生成手順・プロンプト・背景透過・ボイス処理
  05_implementation-status.md … ★実装状況＋シナリオYAML記法の早見表
  06_credits.md               … ★使用素材のライセンス台帳【都度追記すること】
src/
  main.tsx / App.tsx          … エントリと画面の振り分け
  game/
    constants.ts              … 色・キャラ・パラメータ定義【作品の背骨。変更禁止】
    types.ts                  … ドメイン型＋シナリオYAMLの構造定義
    engine.ts                 … シナリオ走査ロジック（React非依存の純粋関数）
    store.ts                  … Zustandストア。全状態を集約
    save.ts                   … localStorageセーブ。スキーマ版とマイグレーション
    flow.ts                   … 物語の入口シーンID
    heroines.ts               … カラットに出すプロフィール（設計書§2.2の抜粋）
    schedule.ts               … 週数・行動枠・デートイベント表
    backgrounds.ts            … 背景キー↔ファイル↔出典の対応表
    scenario/build.ts         … YAML→Sceneの変換と検証（ブラウザ/CLI共用）
    scenario/loader.ts        … ブラウザ側の読み込み（Viteのglob）
  scenario/
    prologue.yaml             … 茜への相談 →「マチアプ始めなよ」
    carat.yaml                … カラット導入・桃果・茜への相談とデレ段階
    encounters.yaml           … ★葵・翠・橙香・紫音との出会い（共通パート）
    dates.yaml                … ★各ヒロインのデートと「重ねの一日」
    endings.yaml              … ★エンディング判定と7つの結末
  ui/
    TitleScreen               … タイトル
    AdvScreen                 … 通常のADV画面。カラット/スケジュールもここに重ねる
    MessageWindow / ChoiceList
    ChatScreen                … LINE風トーク画面（作品の中心）
    CaratCard                 … マッチング演出・プロフィール閲覧
    ScheduleScreen            … 週スケジュール
    StageLayer                … 立ち絵の配置（主人公=左固定／相手=右、話し手を大きく）
    ParamPentagon             … 5色パラメータの5角形表示と変動通知
    sprites/                  … 立ち絵の読み込み。PNG→コード描画→色シルエットの順に落ちる
scripts/
  check-scenario.ts           … シナリオの書式・リンク切れ検証
  smoke.ts                    … 全分岐を走破する再生テスト
  balance.ts                  … ★7エンディングの到達性を総当たりで検証
  fetch-assets.ts             … 背景素材のダウンロード
  remove-bg.py                … 立ち絵の背景透過（reForgeのvenv Pythonで実行）
public/assets/
  bg/                         … 背景。**再配布禁止のためGit管理外**。fetch:assets で取得
  chara/<ID>/<表情>.png       … 立ち絵。置くだけで反映される（Git管理下）
work-assets/                  … 試作・候補・バックアップ（Git管理外）
```

リポジトリ: <https://github.com/Scythercas/irohanihoheto>（public / Scythercasアカウント）

## 3. 世界観・スタイルの固定事項（ブレ防止）

### 3.1 中核コンセプト

**「色」がすべてのメタファー。** 登場人物は名前に対応した髪色を持つ。
主人公 **色葉（いろは）** は"まだ色のついていない葉"＝**無彩色**。5人のヒロインと出会うことで少しずつ"色"を得て（＝魅力レベル上昇）、最後に幼馴染の**茜色**に染まる。

マッチングアプリ名 **「カラット（CARAT）」** は宝石の単位。各ヒロインは色石として扱う。

### 3.2 キャラクター髪色対応表（**変更禁止・絶対遵守**）

| 名前 | 読み | 髪色 | パラメータ | 立場 |
|---|---|---|---|---|
| 色葉 | いろは | 無彩色（灰／黒寄り） | — | 主人公・男性・性的指向は女性。**名前は固定（変更不可）** |
| 茜 | あかね | 茜色（赤／緋色） | 総合（全色平均） | 幼馴染・恋愛感情なしの親友・彼氏なし |
| 葵 | あおい | 青 | **誠実さ** | マッチングアプリで出会うヒロイン |
| 翠 | すい | 翠（緑／エメラルド） | **包容力** | 同上 |
| 橙香 | とうか | 橙（オレンジ） | **ユーモア** | 同上 |
| 紫音 | しおん | 紫 | **感性** | 同上 |
| 桃果 | ももか | 桃（ピンク） | **自信** | 同上 |

※ 髪色はUIカラー・ルート識別色・演出色としても一貫して使用する。
※ **色 ＝ パラメータ ＝ ヒロイン** の三位一体は本作の背骨。**1色特化＝個別ルート／全色平均＝茜ルート**。
※ 各キャラの性格・攻略軸・地雷は `docs/01_game-design.md` §2.2 が正。**セリフを書く前に必ず参照すること。**

### 3.3 プロット骨格（確定）

1. 色葉が茜に「最近彼女欲しいんだよね」と相談する
2. 茜に「じゃあマチアプ始めなよ」と勧められ、マッチングアプリ「カラット」を開始
3. 葵・翠・橙香・紫音・桃果と出会う（それぞれ個性が異なり、攻略方法も異なる）
4. 5人と会うことで主人公の**魅力レベル**が上昇する
5. 分岐:
   - **トゥルー系**: 魅力レベルが条件を満たすと、幼馴染・茜から告白され交際に至る
   - **個別ルート**: プレイヤーの選択次第で5人のいずれかと交際に発展
   - **バッドエンド**: 魅力レベルが一定に達しない場合、茜に惹かれられずゲーム終了

### 3.4 トーン＆マナー

- 現代日本・大学生〜社会人想定（年齢設定は未決）
- マッチングアプリという現代的題材を、**シニカルすぎず・美化しすぎず**描く
- ギャグとシリアスのバランスは未決（Phase1で確認）

## 4. 開発環境の方針（Phase2で確定させる／現時点は制約のみ）

ユーザーからの制約:

- **Unity未経験**（Unityは第一候補としない）
- **制作費は0円**。時間をかけてでも無料手段のみで完成させる方針【確定】
  - ただし **複数アカウント作成は規約違反リスクがあるため採らない**。規約内の無料手段のみを使う
- **WEB公開＋itch.io** で無料公開する
- 一部キャラ（葵）は**知人に声を依頼可能**だが、**ノイズ除去等の音声処理知識はない**
  → 無料ツールでの定型手順はClaude側が用意する

→ Phase2で技術選定し、`docs/03_tech-stack.md` に確定内容を記載する。
→ **一度確定した技術スタックは、ユーザーの明示的な合意なしに変更しない。**

## 4.5 開発コマンドと実装上の約束

```powershell
npm run dev              # 開発サーバー起動（ブラウザで確認）
npm run check:scenario   # シナリオYAMLの書式・未定義キャラ・リンク切れ・未登録背景を検証
npm run smoke            # 全分岐を自動走破。立ち絵の退場・茜ゲート・エンディング判定も検証
npm run balance          # 7つのエンディングすべてに到達できるかを行動枠の総当たりで検証
npm run build            # check:scenario → 型チェック → 本番ビルド
npm run fetch:assets     # 背景素材をダウンロード（Git管理外なのでclone後は必須）
```

立ち絵の背景透過（reForge の venv Python を使う。PIL/numpy が入っているため）:

```powershell
& "C:\StabilityMatrix\Data\Packages\Stable Diffusion WebUI reForge\venv\Scripts\python.exe" `
  scripts\remove-bg.py public\assets\chara\akane
```

**シナリオを書き換えたら `npm run smoke` を通すこと。** ブラウザを開かずに壊れを検出できる。
**しきい値・行動枠・デートの獲得点を触ったら `npm run balance` も通すこと。**
プレイヤーに数値を見せない作りなので、到達不能なエンディングが生まれても人間には気づけない。

### 実装上の約束（守らないと後で必ず壊れる）

1. **`src/game/constants.ts` の色・パラメータ対応は変更しない。** 作品の背骨（§3.2）
2. **状態はすべて Zustand ストアに集約する。** コンポーネント側にゲーム状態を持たせない
   → 状態をまるごとJSON化できることが、セーブ機能の前提になっている
3. **`engine.ts` に React / Vite を持ち込まない。** 純粋関数のままにすることで `smoke` が成立する
4. **セーブデータの構造を変えたら `SAVE_SCHEMA_VERSION` を上げ、`save.ts` の `migrate()` に引き上げ処理を足す**
5. **シナリオはコードに書かない。** すべて `src/scenario/*.yaml` に置く
6. **プレイヤーにパラメータの数値を見せない**（C3②）。5角形の形と言葉（「自信 が 大きく伸びた」）で伝える。数値の併記は `import.meta.env.DEV` の下でのみ
7. 無料素材を使ったら、その場で `docs/06_credits.md` に出典とライセンスを記録する
8. **配布素材（背景）はリポジトリに入れない。** 再配布禁止のため `public/assets/bg/` は `.gitignore` 済み
9. **メッセージウィンドウは常に立ち絵より手前**（z-index 5 / 1）。テキストの可読性を最優先する
10. **エンディングの判定ロジックをシナリオに書かない。** しきい値も優先順位も `engine.ts` の
    `endingRoute()` に集約し、YAMLは `branch` の `ifEnding:` で**結果の名前**だけを受け取る
11. **「重ねの一日」（`*_date_extra`）はヒロインごとに分ける。** 共通の1シーンにすると
    どの色も伸びず、「他の子に会う意味がない＝トレードオフが成立しない」という設計事故になる

## 4.6 収益化に関する制約【重要】

### 現状: **今ある立ち絵のままなら、完全無料でしか出せない**

立ち絵の生成に使ったモデル `illustriousXLPersonalMerge_v30Noob10based` の
Civitai上の許諾が `allowCommercialUse: [RentCivit, Rent]` で、
**`Image`（生成画像の商用利用）が含まれていない**ため。

- 無料配布・パブリックリポジトリでの公開は**問題ない**
- **現行素材のままでの有料販売・投げ銭は不可**

### 方針【確定：2026-08-15】: **商用可のモデルで立ち絵を作り直す**

収益化の選択肢を残すため、`allowCommercialUse` に **`Image` を含むモデル**へ移行する。

- **実施時期は「残り5人の立ち絵に着手するとき」。** 今ある茜7枚は暫定のまま使う
  → 作り直しは新規制作と同じ工程なので、**42枚をまとめて作るのが最も安い**
  → 逆に、**今のモデルで5人を作ってしまうと42枚が丸ごと無駄になる**
- 推奨モデルと手順は **`docs/06_credits.md` §5**、足切り基準は **`docs/04_asset-guide.md` §1**

### モデルを選ぶときの鉄則（同じ失敗を繰り返さないために）

**絵柄より先に `allowCommercialUse` を見る。** `Image` が無ければ採用しない。

```powershell
Invoke-RestMethod "https://civitai.com/api/v1/models/<id>" |
  Select-Object name, @{n='商用';e={$_.allowCommercialUse -join ','}}
```

**素材を追加するときは、必ず商用可否を確認して `docs/06_credits.md` に記録すること。**

## 5. コミュニケーション規約

- やり取りは**日本語**で行う。
- 提案は「懸念点 → 提案 → 選択肢」の順で簡潔に示す。長大な網羅リストは避け、推奨を明示する。
- 質問はまとめて提示し、ユーザーが答えやすいよう番号を振る。
- ユーザーが判断すべき事項を勝手に決めない。ただし定石で済む細部は決めて報告する。

## 6. 変更履歴

| 日付 | 変更内容 |
|---|---|
| 2026-08-14 | 初版作成。Phase1開始。 |
| 2026-08-14 | 第1・第2ラウンドの回答を反映。髪色表に**パラメータ列**を追加し「色＝パラメータ＝ヒロイン」を明文化。主人公名を固定に。**予算を0円に変更**（複数アカウント方式は不採用と明記）。公開先をWEB＋itch.ioに確定。 |
| 2026-08-14 | **Phase1完了、Phase2開始。** タイトルを**『いろはにほへと』**に確定し、いろは歌「色は匂へど散りぬるを」を作品の背骨として最上位に明記。体験版ルートを葵→**桃果**に変更。`docs/03_tech-stack.md` を新設。 |
| 2026-08-14 | **Phase2完了、Phase3（開発）開始。** 技術スタックを**Web自作（Vite + React + TypeScript + Zustand）**に確定。プロジェクト基盤を構築し、ファイル構成・開発コマンド・実装上の約束（§4.5）を追加。Gitリポジトリを初期化。 |
| 2026-08-15 | **体験版先行を取りやめ、製品版を直接作る方針に変更**（K11④）。4人の出会い・7エンディング・エンディング判定（個別ルート優先）・茜のデレ4段階を実装し、**通しで遊べる状態になった**。行動枠を12週×3に、個別のしきい値を80に調整。`npm run balance` を新設。実装上の約束に2項目追加 |
| 2026-08-15 | **茜の立ち絵（7表情）完成。** ファイル構成・開発コマンドを実態に合わせて更新。**§4.6「収益化に関する制約」を新設**（立ち絵の生成モデルが画像販売不可のため、完全無料を確定）。実装上の約束に2項目追加。 |
