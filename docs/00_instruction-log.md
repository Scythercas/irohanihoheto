# 指示履歴ログ（AI Instruction Log）

本ファイルは、ユーザーからAI（Claude）への指示を **一字一句変更せず** 原文のまま保管する記録です。
併せて、各指示に対するClaude側の対応内容を要約して残し、会話の流れを追えるようにします。

## 運用ルール

- ユーザー発言は原文ママ。誤字・空白・改行も含め一切修正しない（コードフェンス内に格納）。
- Claudeの応答は「要約」として記載する（全文は不要）。
- 新しい指示があるたびに、このファイルの末尾に追記する。
- 決定事項は要約に留め、正式な仕様は `docs/` 配下の設計書側を正とする。
- 中断（Interrupted）された発言は、確定指示として扱わず注記のみ残す。

---

## #001 — 2026-08-14 プロジェクト立ち上げ・全体方針の提示

### ユーザー指示（原文ママ）

~~~text
恋愛シミュレーションゲームを作りたい。
AIへの指示履歴を本メッセージは一字一句変更せずにすべてmdに保管して。また、会話の流れがある程度わかるよう、Claudeの対応内容等も要約して説明して。
また、いつセッションを切り替えてもゲームのスタイルや開発環境にブレが生じないよう、必要に応じて設計書やClaude.mdを作成・管理して。

## ゲームの概要

恋愛シミュレーションゲームを作りたい。登場人物は名前に応じた髪の色をしている。
主人公は男で名前は色葉、性的趣向は女性。
恋愛感情はなくただ仲の良い幼馴染の女の子、茜（あかね）がいる。その子は彼氏はいない。
主人公が「最近彼女欲しいんだよね」と茜に相談し、茜に「じゃあマチアプ始めなよ」と勧められたことをきっかけに、マッチングアプリである「カラット」を始める。
アプリをする中で、葵（あおい）、翠（すい）、橙香（とうか）、紫音（しおん）、桃果（ももか）と会う。
最終的には、５人と会うことで、主人公の魅力が引き上げられ、幼馴染に告白されることで付き合うのだが、プレイヤーの判断では、５人のいずれかと交際に発展させることが可能。
５人それぞれ個性があり、交際に発展させたい場合はそれぞれに合わせた攻略方法で進展させないといけない。また、過程で主人公の魅力レベルが一定に達していないと、幼馴染に惹かれることなくゲームが終わってしまう。

##  開発環境

Unityは使ったことがない。
お金を極力かけず、簡単に公開できる環境がいい。WEBでも可。
ボイスやキャラクターデザインについては、必要に応じてAIに課金することは可能。
一部のキャラクターについては私の知り合いにお願いすることも可能だが、ノイズ除去等は知識がない。
　
## 開発手順

以下の手順に沿って、私とのやり取りを進めること。

1. ゲームの内容について、懸念点やより面白くするための提案、深堀のため、質問やヒアリングを実施して。
2. ゲームのあらすじが決まったら、開発環境について相談して。
3. 開発して。
~~~

> 注記: 上記メッセージの送信前に、同趣旨の入力が2回中断（Interrupted）されている。内容は上記に統合・上書きされているため、確定指示は本 #001 のみとする。

### Claudeの対応（要約）

1. **ドキュメント基盤を先に構築**（セッションをまたいでもブレないようにするため）
   - `docs/00_instruction-log.md`（本ファイル）… 指示原文＋対応要約の台帳
   - `CLAUDE.md` … セッション横断の開発憲章。世界観・命名・トーン・作業手順・ドキュメント運用ルールを固定
   - `docs/01_game-design.md` … ゲーム設計書 v0.1（確定事項／暫定案／未決を分離して記載）
   - `docs/02_open-questions.md` … ヒアリング項目と回答の記録簿
2. **手順1（ヒアリング）を実施**。以下の観点で懸念と提案を提示し、質問を投げた。
   - テーマ提案: 主人公「色葉」＝**無彩色**の器。5人（青・翠・橙・紫・桃）と会うことで"色"を得て、最後に**茜色**に染まる。マッチングアプリ名「カラット」を宝石＝色のメタファーとして統一
   - 懸念: ①5人が幼馴染ルートの"踏み台"に見えるリスク ②6ルート分のテキスト量は個人開発として重い ③魅力レベル不足BAD ENDのストレス設計 ④攻略法が手探りになりすぎる問題 ⑤マチアプ題材のリアリティをどこまで描くか ⑥AI生成立ち絵のキャラ一貫性とボイス容量
   - 質問カテゴリ: A.物語/テーマ B.ヒロイン深掘り C.システム D.ボリューム/公開 E.表現レベル F.アセット調達
3. 手順2（開発環境相談）・手順3（開発）は、手順1の回答確定後に着手する方針を明示。

### 決定事項

- ドキュメント駆動で進める（設計書が正、会話は補助）。
- 開発手順は 1.ヒアリング → 2.開発環境相談 → 3.開発 の順を厳守。

### 次アクション

- ユーザーによる `docs/02_open-questions.md` の回答待ち。

---

## #002 — 2026-08-14 ヒアリング第1ラウンド 回答

### ユーザー指示（原文ママ）

~~~text
回答をすべて記録したので確認して。
~~~

> 補足: 実際の回答は `docs/02_open-questions.md` の各表「回答」列にユーザーが直接記入。原文は同ファイルに保持されている。

### Claudeの対応（要約）

1. **全27問の回答を確認**し、設計書 `01_game-design.md` を **v0.2** に更新。
   - 確定: 5色パラメータ制（C1①）／週スケジュール制（C2①）／体験版先行（D2②）／全年齢＋きわどい示唆（E1②）／社会人1〜3年目（A3②）／作中3ヶ月（A4②）／1周2〜3時間（D1②）／WEB＋itch.io公開（D3）／完全無料（D4①）／主人公は立ち絵・ボイスなし（A5①）／AI立ち絵（F1①）／部分ボイス（F2②）／予算〜1万円（F6②）
   - A1で**「5パラメータの色 ＝ 5人の髪色」「1色を磨く→その人と交際／全色平均→茜」**という中核ルールが確定
   - B1が③「全面的に相談したい」のため、**v0.1のヒロイン人物像案は白紙化**し再設計へ
2. **回答間の不整合・要調整点を4件検出**し、第2ラウンド質問（G〜J）を `02_open-questions.md` に追加。
   - **H1（新規懸念R8）**: F4「知人に脇役の声を依頼」↔ B5①「サブキャラ不要」で、**振れる役が存在しない**
   - **H2**: C4③「救済なし」の適用範囲（手動セーブ／オートセーブ／回想の可否）
   - **H3**: C5②「カラット画面は簡易」↔ C6①「LINE風チャットUI」の切り分け
   - **H4**: B4②フェードアウト採用により懸念R1（5人が踏み台に見える）が残存
   - **H5**: B2「ツンデレ＋レベル到達でデレ」の仕様化案の確認
3. **A2②（リアル寄りのほろ苦さ）とE2①（マチアプの負の側面をコミカルに）の配分**を整理。
   コメディ枠を「マチアプあるある」に集約し、人間ドラマはほろ苦く保つ方針を暫定採用。
4. **G**: ヒロイン5人の人物像を、パラメータから逆算した A/B/C の3案ずつ提示。
5. **I**: 予算試算を提示（約3,000〜9,000円 → 1万円以内は現実的）。
6. **J**: タイトル確定・主人公名の変更可否・体験版の対象ルートを質問。

### 決定事項

- ゲームの中核ルール（色＝パラメータ、1色特化＝個別ルート、全色平均＝茜ルート）を確定。
- ヒロイン5人の人物像は再設計。パラメータから逆算するアプローチを採用。

### 次アクション

- ユーザーによる第2ラウンド（G〜J）の回答待ち。**特に H1（知人の声の受け皿）と G2（人物像の選択）が Phase2 の見積もりに直結する。**

---

## #003 — 2026-08-14 ヒアリング第2ラウンド 回答

### ユーザー指示（原文ママ）

~~~text
## Gの回答

- 葵：C
性格はさばさばしていて怒ると怖いがしっかりしていて面倒見が良い。
少し抜けているところがあり、太陽が西から登って東に降りると勘違いしている。
肩にかかるくらいのショートヘアで顔が小さい。
求めるパラメータは、以上を参考に必要に応じて考え直して。
- 翠：B
- 橙香：B
- 紫音：B
- 桃果：C

## Hの回答

1. 知人には葵の声をお願いするが、AI音声を使う可能性もある
2.  a=① b=① c=①
3. ①この切り分けでOK
4. ①＋②
5. ①

## Iの回答

数か月または複数アカウントを用いることで無料での作成が可能であれば、その対応でもよいので無料で作りたい。

## Jの回答

1. ②別案が欲しい。
2. ①色葉固定
3. ①
~~~

### Claudeの対応（要約）

1. **設計書を v0.3 に更新**し、**ヒロイン5人のキャラクターシートを確定**。
   - 葵=C（看護師）＋ユーザー追加設定（サバサバ／怒ると怖い／面倒見が良い／少し抜けている／肩にかかるショートヘア／顔が小さい）
   - 翠=B（花屋・情緒に浮き沈み）／橙香=B（ボケ倒す・プレイヤーはツッコミ役）／紫音=B（美大出身デザイナー・審美眼）／桃果=C（年下の大学生）
2. **葵のパラメータ再検討 →「誠実さ」を維持**。
   追加設定の「少し抜けている」を**誠実さの試験装置**として利用する設計を提案。
   天然発言に対し「①正直に訂正 ②笑って流す ③話を合わせる」の選択肢を用意し、**①を正解**とする。
   サバサバした性格＝取り繕われることを最も嫌う、という論理でキャラ設定とメカニクスを直結させた。
3. **未回答項目を回答から導出**して確定。
   - G1（色↔パラメータ対応）: G2の選択と矛盾しないため提示どおり確定
   - G3（年齢設定）: 桃果C＝年下の学生を選んだ時点で「②ばらけさせる」が確定
4. **確定事項の反映**: セーブ／オートセーブ／回想あり・巻き戻しなし（H2）、メッセージ画面のみ作り込み（H3）、
   別れは最後の一言＋エピローグ後日談で緩和（H4）→ **懸念R1を解消**、茜はしきい値ごとのデレ解放（H5）、
   主人公名「色葉」固定（J2）、体験版は葵ルート（J3）。**予算を「〜1万円」から「0円」に変更**（I）。
5. **「複数アカウントを用いる」案は採用しないことを進言**。
   多くのAIサービスが規約で禁止しており、アカウント停止・生成物の利用権の不安定化リスクがある。
   そのうえで **規約に沿った無料手段のみで0円制作は達成可能**であることを提示（VOICEVOX／ローカル画像生成／
   DOVA-SYNDROME／効果音ラボ／itch.io・GitHub Pages はすべて無料）。「数ヶ月かける」方針はそのまま有効。
6. **第3ラウンド（K1〜K5）を追加**。
   - **K1**: タイトル案6件を提示。推奨は **「無彩色のイロハ」**（主人公名「イロハ」＋「恋愛のイロハ」のダブルミーニング）
   - **K2（新規リスク）**: 知人に依頼する葵は「脇役がよい」という当初意向と逆に、**体験版の主役でセリフ量が最多**。
     テスト録音を先に5本もらう案を推奨
   - **K3**: 0円で画像生成をローカル実行できるか判定するため、**PCのGPU・メモリ構成**を確認
   - **K4**: カラットのナビゲーター音声の要否（推奨: 不要）
   - **K5**: Claudeが暫定で埋めた年齢・職業・髪型の承認。**茜の職業のみ未定**として要相談

### 決定事項

- ヒロイン5人の人物像・攻略軸・地雷を確定（設計書 §2.2）。
- 制作予算を **0円** に変更。ただし複数アカウント方式は不採用とし、規約内の無料手段のみで賄う。
- 体験版は **葵ルート** を先行制作。

### 次アクション

- 第3ラウンド（K1〜K5）の回答待ち。**特に K3（PCスペック）が Phase2 の技術選定・アセット制作手段を左右する。**

---

## #004 — 2026-08-14 ヒアリング第3ラウンド 回答 → **Phase1 完了**

### ユーザー指示（原文ママ）

~~~text
## Kの回答

1. タイトルは、「いろはにほへと」
2. 体験版は桃果にする
3. -
4. ②
5. 何かの事務。髪型は性格に合うように提案して。
~~~

### Claudeの対応（要約）

1. **タイトルを『いろはにほへと』に確定**し、設計書に §1.0 を新設して意味を明文化。
   - 「色葉（いろは）」＋「恋愛のいろは（初歩）」の二重の意味に加え、
     **いろは歌の本歌「色は匂へど 散りぬるを」＝「美しい色も散ってしまう」** が加わることを指摘
   - これにより **B4②「5人とはフェードアウトで終わる」が妥協ではなく作品の主題になる**
   - **懸念R1（5人が踏み台に見える）はテーマ側から完全に回収された**と判断
   - エピローグ（H4②）では「散った5色がその後どう咲いているか」に触れ、無常観として着地させる方針
2. **体験版ルートを葵 → 桃果に変更**（J3①を上書き）。
   K2のリスク（葵＝知人ボイス＋体験版主役＝セリフ最多）に対し、
   **声ではなくルートを変える**解決を採用。葵の知人ボイスは維持したまま負担が下がる。→ **懸念R8解消**
   - 桃果の攻略軸「自信」は「決めてあげる／"どこでもいいよ"は減点」と明快で、チュートリアルとして機能する
   - **新たな懸念R10を追加**: 桃果は21歳・甘え系のため、体験版だけ遊んだ人にA2②（ほろ苦さ）が伝わらない恐れ。
     → 共通パート（茜との導入・カラット開始・他4人との顔合わせ）を体験版に必ず含めて対処
3. **茜を「事務職」に確定**し、髪型を提案。
   - **低めの位置で結んだミディアムのポニーテール（毛先ハネ気味）**
   - 理由: ①事務職のきちんと感と幼馴染の気安さを同居させられる ②顔を背ける／振り返る仕草が映えツンデレと相性が良い
     ③**終盤に「髪をほどいた立ち絵」を1枚だけ用意すれば、差分1枚でデレの段階表現ができる**（0円制作と両立）
4. **K4②を反映**（ナビゲーター音声なし）。K5の他キャラ暫定設定は承認扱いとした。
5. **設計書を v0.4 に更新し、Phase1 の完了を宣言。**
6. **Phase2 に着手**し、`docs/03_tech-stack.md` を新規作成。
   - エンジン候補A〜Dを比較。**本作の中心がチャットUI・スケジュール画面という"アプリ的な画面"である点**を選定の鍵とした
   - **第一推奨: Web自作（TypeScript + React + Vite）**、第二推奨: TyranoScript、Godotは不採用推奨
   - 0円の周辺ツール構成（VOICEVOX／Amuse or ComfyUI+ZLUDA／DOVA-SYNDROME／効果音ラボ／itch.io・GitHub Pages）を提示
   - 質問 T1（エンジン）／T2（Git）／T5（公開優先度）／T6（プログラミング経験）を提示

### 決定事項

- **タイトル『いろはにほへと』確定。** いろは歌を作品の主題として採用。
- **体験版は桃果ルート**（葵から変更）。
- 茜＝24歳・事務職・低めポニーテール（毛先ハネ気味）。
- **Phase1（企画）完了。Phase2（開発環境の選定）へ移行。**

### 次アクション

- `docs/03_tech-stack.md` §6 の **T1・T2・T5・T6** への回答待ち。
- **T1（エンジン選定）が確定次第、Phase3（開発）に着手できる。**

---

## #005 — 2026-08-14 技術選定の回答 → **Phase2 完了・Phase3 着手**

### ユーザー指示（原文ママ）

~~~text
## Tの回答
1. ④
2. する。パブリックで作成でも可
3. ②→①でよいが、itch.ioのメリットを教えて。
4. ③。JS、HTML、CSS、MATLAB、C++等。業務経験もあるので、基本的に勉強すればかけると考えて問題ないが、極力バイブコーディングで済ませたい。
~~~

（上記への回答提示後、同一ターン内で追加の指示）

~~~text
やっぱりWEB自作でよい
~~~

### Claudeの対応（要約）

1. **T1（④＝説明希望）に対し `03_tech-stack.md` §8 を追加**し、3つの論点で深掘り。
   - **§8.1**: ADVに必要な機能16項目を洗い出し、**13〜16（チャットUI・週スケジュール・5色パラメータ・カラット画面）は
     どのエンジンでも自作**になることを提示。本作は**エンジンの恩恵が最も薄い企画**だと結論
   - **§8.2**: 同一シーンを Web自作(YAML) / Ren'Py / TyranoScript の3通りで記述し比較。
     書き味に決定的な差はなく、**差が出るのは13〜16を作るとき**と明示
   - **§8.3【決定打】**: T6の「バイブコーディングで済ませたい」に対し、
     **TypeScript/Reactは学習データが最も豊富でAI生成コードの精度が高い**一方、
     **TyranoScript等のニッチな環境ではAIが動かないコードを出しやすく、デバッグが人間の仕事になる**＝
     バイブコーディングの真逆になることを指摘。「エンジンを使えば楽」は人間が書く前提の話であり、
     **AIに書かせる前提では逆転する**と説明
   - **§8.4**: Web自作のリスク（終わりが見えない／セーブ互換性／ブラウザ差異）と対策を正直に列挙
2. **T5（itch.ioのメリット）に §9 で回答**。GitHub Pages＝**開発・確認用**、itch.io＝**公開・集客用**と役割を整理。
   - **【新規提案 T7】** 日本語のみの作品（D5①）ではitch.ioは海外向けで届く相手が限られるため、
     **ノベルゲームコレクション／ふりーむ**の追加を提案
3. **ユーザーが「やっぱりWEB自作でよい」と決定 → 技術スタック確定。Phase2完了。**
4. **Phase3（開発）に着手し、プロジェクト基盤を構築。**
   - 実行環境を実測（Node v24.18.0 / npm 11.16.0 / Git 2.45.1）
   - Vite + React + TypeScript + Zustand + Howler + js-yaml で土台を作成
   - **シナリオをYAMLで記述する仕組み**を実装。`kind:` を書かずに済むようキーからノード種別を推論し、
     **未定義キャラ・書式ミス・goto/nextのリンク切れを検出**して即座に落とす設計
   - **走査ロジックを `engine.ts` に純粋関数として分離**（React/Vite非依存）。
     これにより**ブラウザを開かずにNode上で全分岐を再生できる**
   - 検証コマンドを2本用意: `npm run check:scenario`（書式検証）／`npm run smoke`（全分岐走破）
   - **プロローグ6シーンを実装**（茜への相談 →「マチアプ始めなよ」→ カラット導入）。
     選択肢で誠実さ/自信が動く分岐を含む
   - タイトル画面は**無彩色の主人公を五色が囲む**構図とし、タイトルの最後の一文字「と」だけを茜色にした
   - `CLAUDE.md §4.5` に**開発コマンドと実装上の約束7項目**を追加（セッション横断のブレ防止）
   - Gitリポジトリを初期化し初回コミット
5. **検証結果**: 型チェック・本番ビルド・シナリオ検証・全分岐スモークテストすべて成功。
   到達シーン 6/6、エンド2種、パラメータ変動も想定どおり。
   **ただしブラウザでの表示は未確認**（環境にブラウザ自動操作ツールがないため）。

### 決定事項

- **技術スタック確定**: Web自作（Vite + React + TypeScript + Zustand + Howler + js-yaml）、
  シナリオはYAML、公開は GitHub Pages（開発） → itch.io（公開）。
- Gitは**パブリックリポジトリ可**。
- **Phase2完了。Phase3（開発）進行中。**

### 次アクション

- ユーザーによる `npm run dev` での表示確認。
- 次の実装対象の決定（体験版に必要な要素の優先順位付け）。
- **T7（国内向け公開先の追加）は完成版のタイミングで判断。**

---

## #006 — 2026-08-14 GitHub公開 ＋ 主要3画面の実装

### ユーザー指示（原文ママ）

~~~text
 gitでパブリックリポジトリを作って。
必ずScythercasアカウントで`Pushして。必要に応じてGlobalアカウントをチェックアウトしてよい

その後、推奨順でよいので開発を実施して。
~~~

### Claudeの対応（要約）

1. **GitHubへの公開**
   - `gh auth status` で2アカウント（`ryo-igarashi-ahs` / `Scythercas`）を確認。
     アクティブは前者だったため `gh auth switch --user Scythercas` で切り替え
   - global の git identity はすでに `Scythercas` だったため、既存コミットの著者も正しいことを確認
   - **<https://github.com/Scythercas/irohanihoheto> をパブリックで作成しpush**
2. **推奨順（チャットUI → カラットUI → 週スケジュール）で実装**
   - **① LINE風チャットUI**（`ChatScreen.tsx`）
     - シナリオに `chat` / `reply` ノードを新設。`from: iroha` なら自分の吹き出し（右側）
     - 過去のやり取りが画面に残り続ける。**返信は入力欄の位置に置き、選ぶと自分の吹き出しになる**
     - 相手が変わると履歴をリセット（＝別スレッドを開いた扱い）
     - 主人公の吹き出しだけ**無彩色**にした（まだ色がついていないという設定に合わせた）
   - **② カラットのカード**（`CaratCard.tsx`）
     - C5②「簡易表現に留める」に従い、画面遷移は作らず1枚のカードのオーバーレイで処理
     - `carat: match` / `carat: profile` の2種。キャラクターカラーの**石**を主役に据えた（宝石＝色石のメタファー）
     - プロフィール文は `heroines.ts` に集約（設計書§2.2の抜粋）
   - **③ 週スケジュール画面**（`ScheduleScreen.tsx`）
     - 12週・行動枠2つ／週。誰と会うかを選ぶと該当シーンへ飛び、枠を消費して週が進む
     - **同じ画面に「主人公が得た色」を帯で表示**。数値は出さない（C3②）
     - デートイベントは `schedule.ts` の `DATE_SCENES` に追記するだけで増やせる構造
3. **シナリオを追加**（`carat.yaml`、15シーン）
   - カラット登録 → スワイプ → 桃果とマッチング → 初トーク（チャット）→ 水族館デート → スケジュールへ
   - **桃果の攻略軸「自信」を実際に機能させた**。「どこ行きたい？」は減点、「日曜、水族館はどう」は加点
   - **茜への相談シーン**を実装し、ヒント役（C3）として機能させた
4. **検証まわりの強化**
   - `build.ts` に **schedule.ts が参照するシーンのリンク切れ検査**を追加
     （ここが抜けると「デートを選んだ瞬間に落ちる」最悪の壊れ方をするため）
   - `smoke.ts` を改修し、**スケジュール画面で打ち切り＋スケジュール経由の各シーンを個別に再生**する方式に変更
     （12週×6択の組み合わせ爆発を避けつつ到達性を担保）
   - セーブ構造を変更したため `SAVE_SCHEMA_VERSION` を **1 → 2** に更新
5. **`docs/05_implementation-status.md` を新設**。
   実装済み／未実装の一覧と、**シナリオYAML記法の早見表**を記載。
   CLAUDE.md のセッション開始手順にも「まずここを読む」を追加した。

### 検証結果

- `npm run check:scenario`: 21シーン／本文116行／チャット15発言／総2,333字 — OK
- `npm run smoke`: **23経路を再生、到達シーン 21/21** — OK
- `npm run build`: 型チェック・本番ビルドとも成功
- **ブラウザ表示は未確認**（環境にブラウザ自動操作ツールがないため）

### 決定事項

- リポジトリは **<https://github.com/Scythercas/irohanihoheto>（public）**。
- チャットUI・カラットUI・週スケジュールの3画面が動作する状態になった。

### 次アクション

- ユーザーによる `npm run dev` での表示確認。
- 実装の次点は `docs/05_implementation-status.md` §5 の優先順（残り4ヒロインとの出会い → 音声再生 → エンディング判定 → セーブ画面等）。

---

## #007 — 2026-08-14 茜の立ち絵作成＋UI3件の修正

### ユーザー指示（原文ママ）

~~~text
茜の立ち絵を作成してみてほしい。
また、以下について修正してほしい。
- 主人公のパラメータは５角形で表示。また、変動があった場合はわかりやすく表示する。
- 主人公と誰かが会話しているシーンは、主人公を左に、会話相手を右に表示する。また、話し手の表示を大きく、聞き手の表示を小さくする。
- 相談できる相手が茜しかいない旨の発言の後、茜が耳を赤らめる表示があるが、まだ主人公にデレデレするには早い。最速でも2人目と知り合い、ある一定の魅力レベルに到達してからにして。
~~~

### Claudeの対応（要約）

1. **茜の立ち絵を作成**（`src/ui/sprites/AkaneSprite.tsx`）
   - 画像生成AIの環境（AMD向け）が未構築のため、**SVGで描画**した。0円・軽量で、表情差分をコードで持てる
   - 設計書§2.2の確定事項に準拠: 茜色の髪／**低い位置のミディアムポニーテール・毛先ハネ気味**／事務職らしいブラウス＋カーディガン
   - **表情7種**（normal / bored / smug / laugh / fluster / away / smile）を目・眉・口の差し替えで実装
   - `hairDown` プロパティを用意し、**終盤の「髪をほどいた」差分**（デレ段階の視覚表現）に備えた
   - `src/ui/sprites/index.tsx` を登録所とし、**実素材ができたら差し替えるだけ**の構造にした
2. **パラメータを5角形（レーダーチャート）表示に変更**（`src/ui/ParamPentagon.tsx`）
   - 塗りを1色にせず、**頂点ごとにそのパラメータの色を置いた5枚の三角形**で構成。
     「色を集めていく」という中核メカニクスを形と色の両方で見せる
   - **変動時**は該当頂点を光らせ、軸ラベルを白く点滅させ、通知を出す
   - **C3②（数値はプレイヤーに見せない）との整合**: 通知は数値ではなく
     **「自信 が 大きく伸びた」「誠実さ が すこし翳った」**という言葉で表す（|差|>=4 で「大きく」）。
     開発時のみ D キーで数値を併記できる
   - スケジュール画面の帯表示もこの5角形に置き換えた
3. **立ち絵の配置を変更**（`src/ui/StageLayer.tsx`）
   - **主人公は常に左、会話相手は常に右**に固定
   - **話し手は等倍で手前、聞き手は0.86倍に縮小して暗く落とす**。地の文のときは両者を中間状態に
   - `engine.ts` に `partner`（右枠の相手）と `faces`（キャラごとの現在表情）を追加。
     `face:` を省略したセリフは**直前の表情を引き継ぐ**
   - **【要確認事項】A5①「主人公は立ち絵なし」と衝突するため、主人公は"顔のない無彩色のシルエット"として実装**。
     テーマ（まだ色がついていない）とも合致する。顔まで描く方針に変える場合は要指示
4. **茜のデレ描写のタイミングを修正**
   - プロローグの「（でも、耳が赤い）」を**削除**し、口の悪い幼馴染として書き直した
     （「重いんだよ、二十四にもなって」「まあ、聞くだけ聞いてやる。奢りだし」）
   - **条件分岐ノード `branch` を新設**し、茜の相談シーンをゲート化
     - `akane_talk_01` を入口にし、`ifMetCount: 2` ＋ `ifTotalParam: 60`（`AKANE_DERE_STEPS[0]`）を満たすときだけ
       `akane_dere_01`（耳が赤い描写を含む）へ。未達なら `akane_talk_plain`
     - `branch` は `ifFlag` / `ifMetCount` / `ifTotalParam` / `ifParam` を指定でき、
       **エンディング判定にもそのまま使える**汎用機構にした
     - 最後の case に条件を書いていないと**検証時にエラーにする**（走査が止まる事故を防ぐため）
5. **検証の強化**
   - `npm run smoke` に**茜ゲートの3ケース検証**を追加（1人目のみ／2人目だが魅力不足／2人目かつ魅力到達）
   - `branch` の行き先も走破対象に追加
   - セーブ構造の変更に伴い `SAVE_SCHEMA_VERSION` を **2 → 3** に更新

### 検証結果

- `npm run smoke`: **茜ゲート3ケースすべて期待どおり**／24経路を再生／到達シーン **23/23** — OK
- `npm run build`: シナリオ検証・型チェック・本番ビルドとも成功
- **ブラウザ表示は未確認**（環境にブラウザ自動操作ツールがないため）

### 決定事項

- **茜が主人公に揺れる描写は、①ヒロイン2人以上と出会い ②総合魅力レベル60到達 の両方を満たすまで出さない**
  （設計書 §2.3 に明記）。
- 主人公の立ち絵は**顔のない無彩色シルエット**として左に配置する（A5①を維持したまま配置要求を満たす）。
- 立ち絵はSVGの仮素材。実素材は `sprites/index.tsx` の登録差し替えで対応する。

### 次アクション

- ユーザーによる `npm run dev` での表示確認（特に茜の立ち絵の出来）。
- 主人公の立ち絵方針（シルエットのままか、顔を描くか）の確認。

---

## #008 — 2026-08-14 PNG立ち絵の要望・レイアウト調整・背景素材の導入

### ユーザー指示（原文ママ）

~~~text
- svgではなく、pngでかわいらしく作成してほしい。トークン節約のため、まずは1枚だけ作成して判断したい
- 立ち絵がセリフに被って見づらいので、立ち絵よりセリフの吹き出しを優先し、透明度を調整して。
- 喫茶店など、背景画像を用意したい。こちらのサイトから必要な素材のダウンロード、もしできなければ、私に指示して。
https://min-chi.material.jp/
~~~

### Claudeの対応（要約）

1. **PNG立ち絵について（対応不可の明示 ＋ 受け入れ準備）**
   - **Claudeはこの環境で画像を生成できない**ことを明示。SVGを描けたのは「コードで描画している」ため
   - そのうえで、**PNGを置くだけで反映される読み込み経路を先に実装**した（`src/ui/sprites/index.tsx`）
     - 優先順位: `public/assets/chara/<id>/<表情>.png` → コードで描いた立ち絵 → 色シルエット
     - 読み込めなければ静かに次の候補へ落ちるので、素材が1枚ずつ増える制作序盤でも画面が壊れない
   - **`docs/04_asset-guide.md` を新設**し、生成に必要な情報を全部そろえた
     - AMD環境（RX 6600 XT）での画像生成AI導入手段の比較。**Amuse AI を第一推奨**
     - **茜の生成プロンプト（英語・SDXL想定）とネガティブプロンプト**を用意。
       髪色がピンク・オレンジ・金髪に転びやすいのでネガティブで潰す指示を明記
     - **キャラ同一性の保ち方**（seed固定 → 表情部分だけ差し替え → img2img → LoRA自作）と、
       表情7種それぞれのプロンプト差し替え表
     - 受け入れ仕様（PNG透過 / 1024×1536 / 配置先 / ファイル命名）
2. **立ち絵とセリフの重なりを解消**
   - メッセージウィンドウを `z-index: 5`、立ち絵レイヤーを `z-index: 1` に固定し、**テキストを常に手前**に
   - ウィンドウの背景を不透明寄りのグラデーションにし、`blur` と上向きの影を追加して境界を明確化
   - 立ち絵は**下端をマスクでフェード**させ、ウィンドウに重なる部分の主張を弱めた
   - 立ち絵のサイズを縮小（34vmin→30vmin / 高さ82%→78%）し、聞き手側の輝度・不透明度をさらに低下
3. **背景素材の導入**（みんちりえ）
   - robots.txt と各素材ページの規約を確認。**商用利用OK／加工OK／クレジット任意／
     素材としての再配布は加工の有無を問わず禁止**
   - ダウンロードURLの規則を特定し、**`npm run fetch:assets` で6枚を取得**
     （居酒屋・夜／一人部屋・夜／カフェ昼・夕方／飲食店・夜／街並み・夕方）
   - **【重要な判断】再配布禁止への配慮**: 本リポジトリはパブリックのため、
     素材をそのまま置くと「素材としての再配布」に当たると判断されうる。
     → `public/assets/bg/` を `.gitignore` し、**各自 `fetch:assets` で取得する方式**にした。
     ビルド成果物への同梱は「作品への利用」なので規約上問題ない
   - `src/game/backgrounds.ts` で bgキー・ファイル・出典を一元管理
   - `npm run check:scenario` に**未登録の背景キーを検出する機能**を追加（エラーにはせず色面へフォールバック）
   - **`docs/06_credits.md` を新設**し、ライセンス台帳の運用を開始
4. **未解決として報告**: みんちりえに**水族館の素材が無い**（桃果の初デート先 `aquarium_evening`）。
   対応は ①デート先を書き換える ②別サイトから探す ③背景なしのまま（現状）の3択。
5. **副次的な混入の報告**: 前回の `git add -A` で `trash/` の**158MBの画面録画**を巻き込んでいた。
   GitHubのサイズ制限で push が拒否されたため**公開はされていない**。コミットを取り消し、
   `.gitignore` に `trash/` を反映して push し直した。

### 検証結果

- `npm run fetch:assets`: 6枚取得成功
- `npm run check:scenario`: 背景キー6種／素材登録済み5（`aquarium_evening` のみ未登録として警告）
- `npm run smoke`: 茜ゲート3件OK／到達シーン 23/23
- `npm run build`: 成功
- **ブラウザ表示は未確認**（環境にブラウザ自動操作ツールがないため）

### 決定事項

- **Claudeは画像生成ができない。** 立ち絵の実素材はユーザー環境の画像生成AIで作る（手順は `docs/04_asset-guide.md`）。
- 配布素材は**パブリックリポジトリに含めない**。取得は `npm run fetch:assets`。
- **テキストの可読性を立ち絵より優先する**（z-index と不透明度で担保）。

### 次アクション

- `docs/04_asset-guide.md` §2 のプロンプトで**茜を1枚生成 → 判断**。
- `aquarium_evening` の対応方針の決定。

---

## #009 — 2026-08-14 水族館素材の追加調達先／立ち絵が退場しない不具合

### ユーザー指示（原文ママ）

~~~text
水族館の背景について、こちらを参考にして。
https://game-materials.com/
今後もみんちりえまたはこのサイトを参考にし、足りない素材があれば打ち上げて。
~~~

（作業中に追加の指示）

~~~text
家に帰ってからも茜の立ち絵が表示されている場合があるので修正して。
~~~

### Claudeの対応（要約）

1. **ゲームまてりあるずを調達先として追加**
   - robots.txt と利用規約を確認。**商用OK／加工OK／クレジット任意／素材としての再配布は禁止**
     （加工後の素材のみの販売も不可。素材はMidjourney生成と明記されている）
   - **水族館の素材が4種見つかった**（水族館7枚／丸い水槽5枚／夜の水族館とハートの水槽4枚 ほか）
   - `backgrounds.ts` を**複数サイト対応**に作り替えた（`source` を判別可能な共用体にし、
     `sourceUrl()` がサイトごとにURLを組み立てる）
   - `aquarium_evening` を登録し取得。**ただし画像の中身はClaudeには見えない**ため、
     候補7枚のプレビューをユーザーに送付して選定を依頼した（暫定で aquarium1 を設定）
   - `docs/06_credits.md` に §0「素材の調達先はこの2サイトに限る」を新設し、両サイトの規約を記載
2. **【不具合修正】背景が変わっても立ち絵が残る問題**
   - 原因: `cursor.partner` はセリフから設定されるだけで、**一度も解除していなかった**。
     居酒屋で茜と別れて自室（`prologue_04`）に移っても茜が立ち続けていた
   - 修正: **背景が変わったら立っていた相手を退場させる**（`setBackground()` に集約）。
     同じ背景を指定し直した場合は続きの場面とみなして退場させない
   - 明示制御用に **`stage: none` / `stage: <キャラID>` ノード**を追加。
     スケジュールから直接飛ぶ `finale_judge` には `stage: none` を置いた
   - **`npm run smoke` に検出機能を追加**。「場所が変わったのに、その場で一度もしゃべっていない相手が
     立ったまま」を自動で見つける（チャット画面は立ち絵を出さないため対象外）

### 検証結果

- `npm run fetch:assets`: 水族館1枚を追加取得（計7枚）
- `npm run smoke`: **立ち絵の退場OK**／茜ゲート3件OK／到達シーン 23/23
- `npm run build`: 成功
- **ブラウザ表示は未確認**（環境にブラウザ自動操作ツールがないため）

### 決定事項

- **背景素材の調達先は「みんちりえ」と「ゲームまてりあるず」の2サイトに限る。**
  見つからない素材があればユーザーに報告して判断を仰ぐ。
- **場所が変わったら立ち絵は自動で退場する。** 例外は `stage:` で明示する。

### 次アクション

- 水族館の候補7枚から採用する1枚の選定（ユーザー判断待ち）。
- 茜の立ち絵PNGの生成（ユーザー環境）。

---

## #010 — 2026-08-15 茜の立ち絵完成・背景透過・ライセンス確定・立ち絵サイズ修正

### ユーザー指示（原文ママ・複数回にわたる）

~~~text
Akaneの画像を作成した。背景透過の方法について具体的に教えて。
~~~

~~~text
手と体、腕と体、髪と首の間等、透過しきれていない部分が多く存在するので、改めて確認して。ただし、Yシャツの白い部分が透過されないように気を付けて。
~~~

~~~text
seedとライセンスを記入した。
商用利用がいけないと書いてあったので、確認してmdに記載して。
https://note.com/kazumu/n/n09f0174d8257#6064c0f9-eb60-4415-8320-ab5bbddbe3e3
~~~

~~~text
その後、キャラの立ち絵が小さくなってしまうので修正して。
ただし、セリフの吹き出しが見づらくなることが無いように気を付けて。
~~~

~~~text
将来収益化したくなった場合の手順について具体的に教えて。
その後、セッションをcompactしたいので、必要な情報がCLAUDE.mdや設計書に含まれているか確認して。
また、今後の開発手順について教えて。
~~~

（この間、Denoising strength は 0.75 がちょうどよかったとの報告あり。当初推奨0.4〜0.6は低すぎた）

### Claudeの対応（要約）

1. **茜の立ち絵7表情が完成**（ユーザー生成）。`public/assets/chara/akane/` に配置され、
   設計書§2.2の確定事項（茜色・低い位置のポニーテール・事務職の服装）をすべて満たしている
2. **背景透過スクリプト `scripts/remove-bg.py` を新規作成**
   - rembgは写真学習モデルで髪の毛先を食うため使わず、**画像の縁からのフラッドフィル**を採用
   - **問題1**: 腕と体・髪と首・手と体の隙間は画像の縁と繋がっていないため消えなかった
     → 囲まれた白領域を**「純白率」**（RGB最小値≥253の割合）で判定する方式を追加。
       実測で背景の隙間90〜97%、Yシャツ・目1.5〜12%と明確に分離
   - **問題2**: 囲まれた領域の切り出しを SOLID_BG(250) と同じにしたところ、
     **シャツ内部が86個の断片に分断され襟元に穴が空いた**
     → `HOLE_CANDIDATE=245` に下げてシャツを陰影ごと1塊にすることで解決（検出数86→3〜5箇所）
   - 検証: 透明率は7枚とも65.2〜65.3%で一致、Yシャツ1,394px残存、取り残し合計5px
3. **ライセンスを原典で確認**（記事の記述と食い違っていたため）
   - **FAIPL-1.0-SD 本文**: Output条項により**生成画像はライセンス対象外**。商用禁止条項なし
   - **Civitai上の作者設定**: `allowCommercialUse: [RentCivit, Rent]` で **`Image` を含まない
     ＝ 生成画像の販売は不可**。これが「商用利用不可」の実体
   - **本作は完全無料公開のため問題なし。ただし投げ銭への切り替えは不可**となった
   - `03_tech-stack.md` の「後から投げ銭に変更できる」という記述を取り消し線付きで訂正
4. **立ち絵が小さい問題を修正**
   - 原因: `width: min(30vmin, 18rem)` と**幅基準**だったため、横長画面では vmin が
     画面の高さを指し、幅が212pxまで縮んでいた
   - **高さ基準（92%）＋ aspect-ratio 768/1152** に変更（実質2倍の大きさに）
   - 吹き出しの可読性維持のため、マスクのフェード開始を62%→80%に深くし、
     **フェードがウィンドウの裏に完全に隠れる**ようにした
   - **潜在バグ発見**: パラメータ5角形に z-index が無く、立ち絵の下に潜る状態だった → `z-index: 4`
5. **収益化の手順を `06_credits.md` §5 に文書化**
   - Civitai APIで**条件を満たすモデルを実データで探索**。`Image` を含むアニメ系を5つ提示
     （**Animagine XL 4.0** が `Image, Rent, Sell` で最も緩く第一候補）
   - **ブロッカーは立ち絵だけ**で、背景・BGM・SE・ボイスはすべて商用可であることを整理
   - **判断は量産前に。今なら7枚、5人ぶん作ってからだと42枚**という時間軸の指摘
6. **compactに備えたドキュメント監査**
   - `CLAUDE.md` のファイル構成が古かったため更新（backgrounds.ts / StageLayer / ParamPentagon /
     sprites/ / fetch-assets / remove-bg.py / public/assets の構造を追加）
   - 開発コマンドに `npm run fetch:assets` と背景透過コマンドを追加
   - **`CLAUDE.md §4.6「収益化に関する制約」を新設**
   - 実装上の約束に2項目追加（配布素材をリポジトリに入れない／ウィンドウは常に立ち絵より手前）
   - `05_implementation-status.md §5` を**5段階のロードマップ**に書き換え

### 決定事項

- **茜の立ち絵（7表情・背景透過済み）が完成。** 本素材として採用。
- **収益化しない方針を確定**（D4①）。投げ銭も行わない。理由と再収益化の手順は `06_credits.md` §5。
- 立ち絵は**高さ基準**でサイズを決める。メッセージウィンドウは常に手前。

### 次アクション

- **ロードマップ第1段階から着手**（`docs/05_implementation-status.md` §5）。
  最優先は**葵・翠・橙香・紫音との出会いシーン**を書いて5人をスケジュールに出すこと。
- 水族館の候補7枚から採用する1枚の選定（未了）。
